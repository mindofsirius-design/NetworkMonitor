<Window x:Class="NetworkMonitor.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Настройки" Height="420" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light"
                                         PrimaryColor="BlueGrey"
                                         SecondaryColor="Cyan"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <StackPanel Margin="24">

        <TextBlock Text="Настройки мониторинга"
                   FontSize="18" FontWeight="Bold" Margin="0 0 0 16"/>

        <!-- Интервал опроса -->
        <TextBox x:Name="IntervalBox"
                 materialDesign:HintAssist.Hint="Интервал опроса (секунды)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0 8"/>

        <!-- Таймаут -->
        <TextBox x:Name="TimeoutBox"
                 materialDesign:HintAssist.Hint="Таймаут ping (мс)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0 8"/>

        <!-- Количество попыток -->
        <TextBox x:Name="RetriesBox"
                 materialDesign:HintAssist.Hint="Количество попыток"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0 8"/>

        <!-- Порог нестабильности -->
        <TextBox x:Name="UnstableBox"
                 materialDesign:HintAssist.Hint="Порог нестабильности (мс)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0 8"/>

        <!-- Тёмная тема -->
        <StackPanel Orientation="Horizontal" Margin="0 12 0 0">
            <ToggleButton x:Name="DarkThemeToggle"
                          Style="{StaticResource MaterialDesignSwitchToggleButton}"
                          Checked="DarkThemeToggle_Checked"
                          Unchecked="DarkThemeToggle_Unchecked"/>
            <TextBlock Text="Тёмная тема"
                       VerticalAlignment="Center" Margin="8 0"/>
        </StackPanel>

        <!-- Звуковые уведомления -->
        <StackPanel Orientation="Horizontal" Margin="0 12 0 0">
            <ToggleButton x:Name="SoundToggle"
                          Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
            <TextBlock Text="Звуковые уведомления"
                       VerticalAlignment="Center" Margin="8 0"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0 20 0 0">
            <Button Content="Отмена"
                    IsCancel="True"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0 0 8 0"/>
            <Button Content="Сохранить"
                    Click="SaveButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

    </StackPanel>
</Window>